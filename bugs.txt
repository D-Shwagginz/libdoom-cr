
map follow command doesnt work to follow player if holding arrow keys

automap doesnt update when in it

chainsaw sound playing after attack

add plutonia tnt pack detection for version printing and stuff

-- Note that check abort has been fixed. Pure doom seems to have put the increment outside of the loop
-- Also fixed finalestage startcast loop bug
-- Also fixed bug not checking to make sure there are at least 2 parameters after -warp on doom 1
-- Fixed midi bug in i_ticksong
  mus_delay = mus_delay * 128 + delay_byte & 0b01111111;
  Fixes into
  mus_delay = mus_delay * 128 + (delay_byte & 0b01111111);
-- Fixed using strcpy instead of concat for M_QuickSave and M_QuickLoad tempstring
-- Fixed quitting not playing sound (i_wait_vbl)
-- Reworked some of the i_(sound) stuff to make positional sound actually track and sounds stop when they should (like DOS doom)
-- Moved return if singletics in net_update to the start to avoid input delay (linuxdoom is so scuffed compared to dos lmao)
-- Fixed init_switch_list not caring about Ultimate Doom switches
-- Fixed i_set_palette to go through gammatable for color values
-- Fixed quit messages to be per game
-- Fixed idclev not working on doom 2 due to returning if episode < 1 or > 1. (Ignore episode number when Doom 2)
-- Fixed automap level names not matching for tnt and plutonia


cast not checking if button is down as well as key (so you can shoot with mouse)
